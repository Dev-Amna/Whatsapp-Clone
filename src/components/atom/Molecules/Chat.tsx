import { View, Text } from "react-native";
import React from "react";
import MessageCard from "./MessageCard";

const Chat = () => {
  return (
    <View>
      <MessageCard />
    </View>
  );
};

export default Chat;
